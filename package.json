{
  "name": "afford-ai",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix code install && npm --prefix code run build && node -e \"const fs=require('fs'); fs.cpSync('code/dist', 'dist', {recursive: true});\"",
    "dev": "npm --prefix code run dev",
    "preview": "npm --prefix code run preview"
  }
}
